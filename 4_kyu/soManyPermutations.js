function permutations(string){
    let result = []
    if string.length === 0{
        return []
    }
    if string.length === 1 {
        return [string]
    }

    for ( let i = 0 ; i < string.length ; i++ ){
        
    }
}